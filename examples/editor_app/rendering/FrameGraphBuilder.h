#pragma once

/*
 * FrameGraphBuilder
 * ------------------------------------------------------------
 * Builds the frame graph for each frame.
 */

class FrameGraphBuilder {
public:
    FrameGraphBuilder();

    void build();
};
