# Theme System

The theme system allows dynamic UI styling.

---

## Features

- Multi-theme support per application
- Time-based theme switching
- Smooth transitions
- Gradient-based UI rendering

---

## Theme Structure

```cpp
struct Theme {
    Color background;
    Color foreground;
    float opacity;
};
